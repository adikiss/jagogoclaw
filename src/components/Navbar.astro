---
import { useTranslations, getLocaleFromUrl, getAlternatePath } from '@i18n/utils';
import { locales, localeLabels } from '@i18n/ui';
import Button from './ui/Button.astro';
import Logo from './ui/Logo.astro';
import { site } from '@data/site';

const locale = getLocaleFromUrl(Astro.url);
const t = useTranslations(locale);

const navLinks = [
  { href: '#curriculum', label: t('nav.curriculum') },
  { href: '#pricing', label: t('nav.pricing') },
  { href: '#instructor', label: t('nav.instructor') },
];

const otherLocale = locales.find((l) => l !== locale)!;
const langSwitchHref = getAlternatePath(Astro.url, otherLocale);
---

<header
  id="site-header"
  class="sticky top-0 z-50 w-full border-b border-transparent bg-white/80 backdrop-blur-md transition-colors duration-200"
>
  <nav class="mx-auto flex h-16 max-w-6xl items-center justify-between px-4 sm:px-6 lg:px-8">
    <a href={locale === 'id' ? '/' : `/${locale}/`} class="flex items-center" aria-label="Jago GoClaw">
      <Logo size={28} />
    </a>

    <div class="hidden items-center gap-8 md:flex">
      {navLinks.map((link) => (
        <a
          href={link.href}
          class="text-sm font-medium text-slate-600 transition-colors hover:text-brand-600"
        >
          {link.label}
        </a>
      ))}
    </div>

    <div class="flex items-center gap-3">
      <a
        href={langSwitchHref}
        class="hidden rounded-lg px-2.5 py-1.5 text-xs font-semibold text-slate-500 ring-1 ring-inset ring-slate-200 transition-colors hover:bg-slate-50 hover:text-slate-700 sm:inline-block"
        hreflang={otherLocale}
      >
        {localeLabels[otherLocale]}
      </a>

      <div class="nav-guest hidden items-center gap-3 sm:flex">
        <a
          href="/masuk"
          class="text-sm font-semibold text-slate-600 transition-colors hover:text-brand-600"
        >
          {t('nav.login')}
        </a>
        <Button href={site.registerUrl} size="sm">
          {t('nav.cta')}
        </Button>
      </div>

      <div class="nav-user hidden items-center gap-2 sm:flex">
        <a
          href="/course"
          class="inline-flex items-center gap-1.5 text-sm font-semibold text-brand-600 transition-colors hover:text-brand-700"
        >
          <svg class="h-4 w-4" fill="none" viewBox="0 0 24 24" stroke-width="1.8" stroke="currentColor">
            <path stroke-linecap="round" stroke-linejoin="round" d="m15.75 10.5 4.72-4.72a.75.75 0 0 1 1.28.53v11.38a.75.75 0 0 1-1.28.53l-4.72-4.72M4.5 18.75h9a2.25 2.25 0 0 0 2.25-2.25v-9a2.25 2.25 0 0 0-2.25-2.25h-9A2.25 2.25 0 0 0 2.25 7.5v9a2.25 2.25 0 0 0 2.25 2.25Z" />
          </svg>
          {t('nav.myCourse')}
        </a>
        <form method="POST" action="/api/participant/logout">
          <button
            type="submit"
            class="rounded-lg px-2.5 py-1.5 text-sm font-semibold text-slate-500 ring-1 ring-inset ring-slate-200 transition-colors hover:bg-rose-50 hover:text-rose-600 hover:ring-rose-200"
          >
            {t('nav.logout')}
          </button>
        </form>
      </div>

      <button
        id="mobile-menu-toggle"
        type="button"
        class="inline-flex items-center justify-center rounded-lg p-2 text-slate-600 hover:bg-slate-100 md:hidden"
        aria-label="Toggle menu"
        aria-expanded="false"
      >
        <svg class="h-6 w-6" fill="none" viewBox="0 0 24 24" stroke-width="1.5" stroke="currentColor">
          <path stroke-linecap="round" stroke-linejoin="round" d="M3.75 6.75h16.5M3.75 12h16.5m-16.5 5.25h16.5" />
        </svg>
      </button>
    </div>
  </nav>

  <div id="mobile-menu" class="hidden border-t border-slate-100 bg-white md:hidden">
    <div class="space-y-1 px-4 py-3">
      {navLinks.map((link) => (
        <a
          href={link.href}
          class="mobile-menu-link block rounded-lg px-3 py-2 text-base font-medium text-slate-700 hover:bg-slate-50"
        >
          {link.label}
        </a>
      ))}
      <a
        href={langSwitchHref}
        class="block rounded-lg px-3 py-2 text-sm text-slate-500"
        hreflang={otherLocale}
      >
        {localeLabels[otherLocale]}
      </a>

      <div class="nav-guest mt-2 border-t border-slate-100 pt-3">
        <a
          href="/masuk"
          class="mobile-menu-link block rounded-lg px-3 py-2 text-base font-medium text-slate-700 hover:bg-slate-50"
        >
          {t('nav.login')}
        </a>
        <Button href={site.registerUrl} class="mt-2 w-full">{t('nav.cta')}</Button>
      </div>

      <div class="nav-user mt-2 hidden border-t border-slate-100 pt-3">
        <a
          href="/course"
          class="mobile-menu-link block rounded-lg px-3 py-2 text-base font-semibold text-brand-600 hover:bg-brand-50"
        >
          {t('nav.myCourse')}
        </a>
        <form method="POST" action="/api/participant/logout">
          <button
            type="submit"
            class="mt-2 w-full rounded-xl bg-rose-50 px-4 py-2.5 text-sm font-semibold text-rose-600 transition-colors hover:bg-rose-100"
          >
            {t('nav.logout')}
          </button>
        </form>
      </div>
    </div>
  </div>
</header>

<script>
  const header = document.getElementById('site-header');
  const toggle = document.getElementById('mobile-menu-toggle');
  const menu = document.getElementById('mobile-menu');

  const onScroll = () => {
    if (window.scrollY > 8) {
      header?.classList.add('border-slate-200', 'shadow-sm');
    } else {
      header?.classList.remove('border-slate-200', 'shadow-sm');
    }
  };
  onScroll();
  window.addEventListener('scroll', onScroll, { passive: true });

  toggle?.addEventListener('click', () => {
    const expanded = toggle.getAttribute('aria-expanded') === 'true';
    toggle.setAttribute('aria-expanded', String(!expanded));
    menu?.classList.toggle('hidden');
  });

  document.querySelectorAll('.mobile-menu-link').forEach((el) => {
    el.addEventListener('click', () => {
      toggle?.setAttribute('aria-expanded', 'false');
      menu?.classList.add('hidden');
    });
  });

  fetch('/api/participant/session')
    .then((r) => r.json())
    .then((d) => {
      if (!d?.authenticated) return;
      document.querySelectorAll('.nav-guest').forEach((el) => el.classList.add('hidden'));
      document.querySelectorAll('.nav-user').forEach((el) => {
        el.classList.remove('hidden');
        el.classList.add('flex');
      });
    })
    .catch(() => {});
</script>
